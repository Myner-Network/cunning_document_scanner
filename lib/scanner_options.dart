/// Flash mode for the document scanner camera.
enum FlashMode {
  /// Flash is automatically enabled based on lighting conditions.
  auto,
  
  /// Flash is always on during scanning.
  on,
  
  /// Flash is always off during scanning.
  off,
}
